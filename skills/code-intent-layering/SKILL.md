---
name: code-intent-layering
description: "コード・テスト・コミットメッセージ・コードコメントへ意図を正しいレイヤーに配置するガイド。コードは How、テストは What、コミットメッセージは Why、コードコメントは Why-not と局所的な Why を担う。実装コードやテストを書く・リファクタリングするとき、コミットメッセージを作成するとき、コードコメントを書くか消すか迷うとき、コードレビューで説明の置き場所を指摘するときに必ず使用する。「コメントを書く」「コメントを直す」「Why を残す」「Why-not」「コミットメッセージ作成」「リファクタリング」「コードレビュー」などが対象。"
---

# 意図の置き場所ガイド — 説明はメンテナが探しに行く場所へ

将来のメンテナは、仕組みはコードで、契約はテストで、経緯はコミットメッセージで、制約はコメントで探す。その期待に合わせて情報を配置する。

- **コード: How** — 振る舞いをどう実装しているか。
- **テスト: What** — どの振る舞いが成立しなければならないか。
- **コミットメッセージ: Why** — なぜその変更を入れたのか。
- **コードコメント: Why-not** — なぜ単純化・代替案・一見よさそうな変更を採らないのか。
- **コードコメント: 局所的な Why** — その場で読まないと誤編集につながる、非自明で失われやすい理由。

これは厳格な禁止ルールではなく、保守性を上げるためのヒューリスティックである。原則の置き場所に寄せつつ、文脈を移動すると危険な場合だけ例外を認める。

## ワークフロー

1. 書く・読む対象が、実装コード、テスト、コミットメッセージ、コードコメントのどれかを確認する。
2. その成果物が標準で答えるべき意図（How / What / Why / Why-not）を確認する。
3. 置き場所がずれている説明は、より適切なレイヤーへ移す。
4. その場に残さないと誤解や危険な編集を招く説明だけ、例外として残す。

## レイヤールール

### コード: How

実装コードでは、仕組みが自然に読める状態を目指す。

- 説明コメントに頼る前に、名前、型、小さな関数、明示的な制御フローで表現する。
- ドメイン用語や型で意図を運べるなら、コメントではなくコード構造に寄せる。
- アルゴリズムに直接影響しない広いプロダクト事情や判断理由は、実装本体へ埋め込みすぎない。

### テスト: What

テストでは、観測可能な振る舞いと守るべき契約を明確にする。

- テスト名には条件と期待結果を書く。
- 結果、不変条件、契約、境界値、回帰条件を検証する。
- 低レベルな契約を固定する意図がない限り、実装手順の複製にならないようにする。

### コミットメッセージ: Why

コミットメッセージでは、diff だけでは復元しにくい動機と判断を残す。

- ユーザー価値、プロダクト事情、運用上の制約、技術的負債、障害対応など、変更理由を書く。
- なぜ今なのか、なぜこの方向なのかを必要に応じて補足する。
- diff の要約だけで終わらせない。何が変わったかは diff が示している。

### コードコメント: Why-not と局所的な Why

コードコメントは、コードだけでは読み取れない制約を残す場所として使う。

- **Why-not** を書く: 却下した代替案、危険な単純化、過去の落とし穴、互換性制約、性能上の崖、順序依存、セキュリティ境界など。
- **局所的な Why** も書いてよい: その理由をコードのそばで読めないと、将来の編集者が誤って壊しそうな場合。
- コードの How を言い換えるだけのコメントは消す。How の言い換えはコードと二重管理になり、コードだけが変更されると嘘になるため。
- 「自分が何を考えたか」より、「この場に存在する制約」を書く。

コメントを書く前の問い:

- 「なぜもっと単純にできないのか？」
- 「どんな将来の変更がここを壊すのか？」
- 「メンテナが試しそうな代替案は何で、なぜここでは誤りなのか？」
- 「名前、型、テスト、コミット履歴から復元できない局所的な理由は何か？」

## 実例集

判断に迷ったら [references/examples.md](references/examples.md) を参照する。コメント（How の言い換え / Why-not のバリエーション / 局所的な Why）、テスト、コミットメッセージそれぞれの良い例・悪い例を収録している。

最小の例:

```ts
// 悪い例（How の言い換え — 消す）:
// ユーザーIDでキャッシュを検索する
const cached = cache.get(userId);

// 良い例（Why-not — 残す）:
// Map ではなく LRU を使う。セッション数は無制限に増えるため、
// 素朴な Map に置き換えるとメモリリークが再発する（#482 参照）。
const cached = lruCache.get(userId);
```

## レビュー用チェックリスト

実装やレビューでは次を確認する。

- コードは、文の言い換えコメントなしで **How** を読める。
- テストは、ユーザーや呼び出し元が依存できる **What** を示している。
- コミットメッセージは、**なぜ今なのか**と**なぜこの方向なのか**を説明している。
- コメントは、**Why-not** または失われやすい局所的な **Why** を説明している。
- 同じ説明を複数箇所に置く場合、その重複が読み手の判断に役立つ。

## コミットメッセージの型

コミット作成を依頼されたら、次の形を基本にする。

```text
<命令形の変更要約>

なぜこの変更が必要なのか、どの制約や問題に対応しているのか、
なぜこの方向を選んだのかを書く。将来の保守に効く場合だけ、
重要な却下案も補足する。
```
